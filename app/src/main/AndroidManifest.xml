<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.svu.e_we_job">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.CandidateActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:label="@string/new_company" android:name=".view.activity.RegisterCompanyActivity" />

        <activity android:label="@string/register_candidate" android:name=".view.activity.RegisterCandidateActivity" />

        <activity android:name=".view.activity.CompanyActivity" />

        <activity android:name=".view.activity.LoginActivity" android:label="@string/login"/>

        <activity android:name=".view.fragment.SuitableCandidatesActivity" android:label="@string/suitable_candidates"/>
    </application>

</manifest>